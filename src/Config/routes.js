// all routes excluding the / index route and fallback 404 route

const routes = {
    "/": "Home",
    "/projects": "Projects",
    "/bunq": "BunqCharts",
    "/404": "NotFound"
};

module.exports = routes;
